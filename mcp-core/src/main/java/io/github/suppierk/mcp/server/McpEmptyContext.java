package io.github.suppierk.mcp.server;

/** Supplies an explicit application context when a server application has no invocation data. */
public enum McpEmptyContext {
  /** The single empty application-context value. */
  INSTANCE
}
